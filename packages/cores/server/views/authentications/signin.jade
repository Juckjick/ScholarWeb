extends auth

block auth
  form.signin(action="/users/session", method="post")
    .form-group
      label(for='username') รหัสบัตรประจำตัวประชาชน
      input.form-control#username(type='text', name="username", placeholder='รหัสบัตรประจำตัวประชาชน')

    .form-group
      label(for='password') รหัสผ่าน
      input.form-control#password(type='password', name="password", placeholder='รหัสผ่าน')

    button.btn.btn-primary.pull-left(type='submit') เข้าสู่ระบบ
  a.pull-right(href='/register') สมัครสมาชิก
